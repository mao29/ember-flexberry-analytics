import reportTranslations from "ember-flexberry-analytics/locales/ru/translations";
import Ember from "ember";

const translations = {};
Ember.$.extend(true, translations, reportTranslations);

export default translations;